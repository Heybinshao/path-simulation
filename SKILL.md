---
name: path-simulation
description: "【路径模拟自检法】写完/改完 workflow、skill、教程、开发方案（plan）后，选真实用户指令模拟执行找卡点。触发词：路径模拟、走一遍、simulate workflow、验收方案。适用：可执行流程类内容（skill/workflow/教程/方案）；不适用：纯知识类内容。"
version: 2.7.1
author: 彬少
platforms: [linux, macos, windows]
metadata:
  hermes:
    tags: [quality, verification, workflow, skill, testing]
    category: binshao
    triggers: [路径模拟, 走一遍, path simulation, simulate workflow]
---

# 路径模拟自检法

写完/改完 workflow、skill、教程、提示词规范后，不能只静态检查结构格式。先做使用者场景枚举（步骤 0，查入口覆盖），再选真实用户指令按步骤模拟执行（查流程断链），看哪里会卡住、哪里逻辑断链、哪里触发条件没覆盖。至少走 3 条不同路径（标准 + 至少 1 条风险路径：异常 #6 / 跨文件 #7 优先），发现问题改完后再换路径重走。按第八步的分级停止标准收工。

---

## 适用边界（先读这个）

| 适合 | 不适合 |
|------|--------|
| skill / workflow / 教程步骤 / 操作手册 | 纯知识类内容（百科条目、观点文章） |
| 有"用户从 A 到 B"流程的东西 | 没有执行步骤的参考文档 |
| 用 AI 写流程的人（agent 提示词、自动化脚本） | 只给别人读不给人用的文档 |

**一句话判断**：这东西有没有"用户会走的一串步骤"？有 → 值得路径模拟；没有 → 静态检查够了。

**判定示例（边界靠例子才具体）：**

| 被测对象 | 判定 | 为什么 |
|---------|------|--------|
| 一个 skill：用户提供 epub → 得到拆解文档 | ✅ 走路径模拟 | 有明确「用户从 A 到 B」流程，且多入口（单本/批量/跳过移动） |
| 一篇观点文章（纯知识） | ❌ 静态检查够 | 没有执行步骤，读者只读不走 |
| 一份 API 参考文档（只查不操作） | ❌ 静态检查够 | 无「用户操作序列」，只有查询语义 |
| 一段自动化脚本：输入文件 → 清理 → 输出 | ✅ 走路径模拟 | 有输入输出链，异常路径（文件不存在/格式错）需要验证 |
| 一个提示词模板（用户填字段 → 生成） | ✅ 走路径模拟 | 有「填空 → 产出」流程，跳过字段/空值/错格式都是路径 |
| 一个 Obsidian 工作流（新建 → 命名 → 归档） | ✅ 走路径模拟 | 有步骤序列 + 中间状态（改名/反悔/覆盖） |
| 本 skill 自身（自指测试） | ✅ 可测 | 坑表回写走步骤7 自己分支（改自己的 references 无越权问题），其余流程不变 |

**两个必走的触发信号**（不止「有没有步骤」）：
1. 内容里有「用户输入 → 处理 → 输出」的链条 → 值得走
2. 内容里有「分支/例外/可选步骤」→ 必须走（分支就是路径）

---

## 核心机制

**为什么叫「路径」？** 用户进入系统的方式不止一种：「直接执行」「先看方案再执行」「跳过某步」「中途反悔」——每一种用户意图，都是一条不同的路径。

**一句话定义：** 路径模拟就是用户可能的每种用法都跑一遍，看哪会卡住。

- 静态检查相信文档写清楚了。
- 路径模拟不相信——它要真的走一遍才信。

---

## 执行步骤

### 0. 使用者场景枚举（路径模拟的横向轴，先于一切路径）

路径模拟（纵向）沿一条指令走到底查断链；这一步（横向）先穷举「谁、说什么话、要什么结果」，查**有没有整类用户/说法/需求根本没被覆盖**。实证（2026-09-05 book-distiller 走查）：3 条路径全过后，横向扫描仍抓出 3 项——触发词缺「整合」（说「整合」的用户整类进不来）、归档动作悬空（流程走到「用户确认后归档」就断）、模板占位符说明漏列。这三项任何单条路径都走不出来。

穷举维度（按被测对象选适用的）：
- **入口词**：用户可能用什么词触发？（动词同义词、口语变体、中英文）——漏一个词就是漏一整类用户
- **用户身份/状态**：新用户（无 config/无历史）/ 老用户 / 并发会话用户 / 别人拿去用（第三方）
- **输入形态**：标准输入 / 边缘输入（超长名、繁体、缺扩展名、空文件夹、重复文件）
- **意图分支**：要完整结果 / 只要重点 / 中途改主意 / 拆完还要下一步（衔接后续流程吗？）
- **产物去向**：产物生成后用户下一步想干什么？（归档/发布/再加工）——流程在哪一步断掉

产出：场景矩阵表（每行 = 用户类型 × 说法 × 期望结果 × 被测对象是否有指引）。**矩阵里「无指引」的行就是横向缺口**，先补文档/触发词，再进入步骤 1 选路径。

### 1. 选一条用户指令作为入口

从实际用户可能的说法里选，不要自创。**优先从步骤 0 的场景矩阵里挑「有指引」的行当路径入口**（矩阵「无指引」的行此时应已补过）。常见入口：

> 若指定的被测对象文件/路径不存在或无法读取，提示使用者重选有效入口，不进入走查（异常兜底，呼应风险路径 #6）。

- 标准路径：用户按文档预期的方式使用
- 跳过路径：用户跳过某步或直接要求结果
- 分批路径：用户分多次执行、中间插入其他请求
- 中断路径：用户中途反悔、改口、撤销
- 反悔路径：执行到一半用户说「不对，回退」

### 2. 按步骤实际走一遍（循环至少 3 条路径）

沿着规范每步往下走，走到哪发现：

- **卡住** — 这一步没有输入，或输入和上一步输出对不上
- **歧义** — 需要读者自己去猜，规范没说清
- **缺失** — 某个分支没覆盖

这些问题只在「真的走」时才暴露。走到每步时问自己：「这一步需要的输入从哪来？如果我是第一次用的用户，我能看懂吗？」

**循环走至少 3 条不同路径（标准 + 至少 1 条风险路径：异常 #6 / 跨文件 #7 优先）**，每走完一条只记录问题、不报告不修改，全部走完后统一进入步骤 4。

### 3. 记录每个问题

每条路径走完，把发现的问题记下来：

- 哪一步卡住
- 为什么卡（触发条件没写/输入缺失/输出对不上）
- 建议怎么修

### 4. 汇总报告给使用者（强制，先于任何修改）

**至少 3 条路径全部走完后**，把完整问题清单一次性展示给使用者，不直接动手修。格式按 `references/report-template.md` 输出，要点：问题清单表（# / 路径 / 问题 / 严重度 / 哪步卡 / 为什么 / 建议修复）+ 严重度定义（阻断 > 一般 > 建议，按爆炸半径排序，先报阻断）+ 走查轨迹（每条路径必填含无问题路径，「走到第 X 步，第 Y 步判断无卡点」——防漏走、可复盘）。结尾必须带「共发现 N 个问题。是否修复？」

**为什么必须先报告**：路径模拟发现的"问题"可能不是 bug，而是作者故意的设计（某步看似多余其实是安全闸门）。直接修 = 替作者做设计决定。使用者需要先看到问题清单，才能判断修不修、怎么修。

**🔴 报告先于修复是不可妥协纪律（实测违反）**：本纪律对**每一轮走查的每一个新发现**生效，不存在豁免——同会话早前拍板过大方向、问题看起来是「自己刚写的行」、改动看起来「机械」，都不构成跳过理由。2026-09-05 自指走查实测：执行者以效率为由跳过报告直接修，实际后果是坑表加错位置 + 格式不符，全靠重走才抓出；复盘发现违反两层纪律（迭代内容先斩后奏 + 自检发现跳过程序）。**发现问题的当下就出清单等确认，效率话术不构成例外**（完整案例见 references/common-pitfalls.md「修复的授权以轮次为单位」）。

### 5. 询问后执行修复（不自动修）

展示清单后，**等使用者决定**，不自动执行：

- 使用者说「修」/「全部修」→ 按建议批量修复
- 使用者说「只修 X」→ 只修指定条目
- 使用者说「都不修」→ 跳过修复，仅保留报告
- **自用场景可轻量确认**：「发现 N 个问题，直接按建议修？」→ 一次确认批量执行（效率优先，但报告不可省——你也会遇到"这问题其实是故意的"）
- **使用者主动授权「直接修/不用问」→ 可跳过报告与逐条确认**，直接按建议批量修复；修复记录仍写入产出物（见 references/report-template.md，含「使用者决定」节，此时填「使用者授权直接修」）

### 6. 修完换路径重走

**验证手法优先级：脚本断言 > 人工重走。** 先问「这个修复能不能写成脚本断言？」——数值/口径/存在性类修复（如计算口径、文件字段、残留检查）能写成一跑出结果的断言，结果可复现零主观；语义/流程类修复才用人工重走。两者可叠加：脚本实锤数值项 + 重走确认流程项。

**负例测试（验证「能抓坏」）**：验收脚本/校验规则修完后，除了跑正例，还必须喂**故意坏的输入**确认能拦住——错配书单、超下限数值、错误格式，断言退出码/报错符合预期。只验「检查通过」不验「能抓坏」，脚本可能永远绿灯但实际什么都拦不住（2026-09-05 book-distiller 实测：正例全过 + 负例喂错书单才验出 QC 真能抓缺书）。

**回归用例集固化**：修完把本次的输入→期望输出固化为用例清单（存在脚本注释或用例文件里），增量修改后**重跑整套固定用例**，不是每次现编一两个——防改 A 坏 B（2026-09-05 实测：clean_title 加扩展名剥除后，重跑 5 个历史用例确认零回归）。

至少走 3 条不同路径（标准 + 至少 1 条风险路径：异常 #6 / 跨文件 #7 优先；被测对象是 agent 判断流程时，可用会话回放法取材，见 references/path-types.md 路径来源二）。不要只走一条就停。

**部分修复时的处理**：使用者只修了部分条目（「只修 X」），重走时未修的问题还会出现——报告中标注「使用者选择不修（原问题 #N）」，**不重复询问**，除非使用者改口。重走目标是验证已修条目不再卡 + 找新路径的新问题，不是重新审未修条目。

### 7. 沉淀新坑（可选，必须先询问使用者）

修复后，把「新发现的坑类型」回写进被测试 skill 的陷阱表/常见坑表，防止同类再犯。

**⚠️ 回写前必须询问使用者，不能自动做**——修改被测试 skill 文件是有副作用的操作：

- 询问内容：「发现 N 个新坑，是否回写进 <skill 名> 的陷阱表？」
- 使用者确认 → 回写；使用者拒绝 → 只修当下问题，不沉淀
- **被测试 skill 不是使用者自己的（官方 skill / 导入的第三方 skill / 共享 skill）→ 默认只报告不写入，除非使用者明确要求**
- 已确认回写时，**一律回写进 references/，不写进 SKILL.md 正文**（坑表是给修改者看的，不是给使用者看的——写进正文会让使用 skill 时加载维护向内容）：
  - 已有通用陷阱表/常见坑表文件 → 追加进该文件（先查重，已有坑不重复加）
  - 没有 → 新建 `references/common-pitfalls.md`（目录不存在则创建）
  - **无论哪种，都必须在主文件加引用**（防「拆分后引用断链」）

**为什么必须先问**：路径模拟的职责是发现和报告，不是替使用者决定改不改别人的/共享的 skill。沉淀是价值，擅自改是越权。

### 8. 停下来的标准

**分级停止**：新路径的发现仅剩「建议」级、且每条都有了明确处置（修/不修/记录）→ 允许停。存在「一般」及以上未闭环的问题 → 必须继续走。「没发现问题」本身不是终点——换新路径再探，直到命中上述停止条件。**重走轮次每轮 ≥1 条新路径即可，累计覆盖不变。**（若被测对象含多个参考文件，停止前必须至少走一次跨文件路径 #7，专门核对文件间矛盾。）

---

## 参考文件

- `references/path-types.md` — 8 条路径来源清单（经典 7 条用户指令路径 + 会话回放法，选入口时查）
- `references/report-template.md` — 产出物报告模板（输出时查）
- `references/faq.md` — 常见问题（卡住时查）
- `references/walkthrough.md` — 真实走查范例（一条完整 3 路径走查长啥样，起步时查）
- `references/common-pitfalls.md` — 实测坑沉淀（现 7 坑：多轮修订脱节、方案边界画小、报告快照过期、并发会话抢改、模拟器断言未复核、正则初报当结论、修复授权以轮次为单位；测 plan/方案类文档前先读）

## 和「自检清单」的关系

**🔴 一条前置纪律（先读这个再往下走）**：路径模拟产出的问题清单**必须先报告、等使用者确认后才修**（步骤 4→5）——对本会话每一轮新发现都生效，同会话拍板过大方向不等于授权后续修复（2026-09-05 实测违反案例见 references/common-pitfalls.md）。

两者互补，不是替代：

- **自检清单** — 查单点（缩写/密度/违禁词）
- **路径模拟** — 查流程（触发条件/输入输出/断链/方法论不匹配）

顺序：自检清单查单点 → 路径模拟查流程 → 再自检清单确认。

---

## 常见坑

### 一、本方法能查出的流程 bug（被测对象里的问题）

| 坑 | 表现 | 修法 |
|--|------|------|
| 时序错位 | 检查/确认类步骤排在**写入/分发之后**，等于没查（如覆盖确认写在最后一步，但文件在第二步就已被写完） | 检查类步骤必须排在对应写入/分发**之前**。写完再查的「检查」是装饰 |
| 跨文件矛盾 | 每条路径都走通了，但相关参考文件之间互相矛盾（A 文件说允许、B 文件说禁止） | 至少检查一次**跨文件一致性**：走完 N 条路径后，专门检查多个参考文件中描述同一概念的部分 |
| 异常表误导 | 异常处理表声称「A 正常」，但真实路径下 A 也出错 | 异常表描述必须来自实测，不能凭想象写；重走路径时专门验证异常表里的"正常"声明 |
| 主体可移除测试 | 去掉工作流中的某个角色/步骤后整个流程依然成立 | 该元素是装饰性的——重新设计：移除或让它承担真正的功能 |
| 方法论误移 | 发现的问题不是"实现错误"，而是整套方法论不匹配内容域 | 此时修的不是 workflow 步骤，是 workflow 本身的设计前提。回到被测试对象的定位重新评估——这条路走不通不是因为 step 写错了，是因为路本身就不该修成这个样子。记录方法论不匹配作为独立的审查项（见「方法论审查三问」） |

### 二、使用本方法的误用（agent / 人照做时容易犯的）

| 坑 | 表现 | 修法 |
|--|------|------|
| 静态审查通过就信了 | 结构完整就认为没问题 | 必须走一遍，至少 3 条路径（且须含标准 + 至少 1 条风险路径：异常/跨文件优先） |
| 只走一条路径 | 标准路径通关就收工 | 换入口再走 |
| 走一遍没问题就停 | 第一次走通就完成 | "没发现问题"不是终点，再换路径 |
| 自检清单代替路径模拟 | 以为逐项打勾就够了 | 清单查单点，模拟查流程，两者缺一不可 |
| **测试域污染** | 模拟时用了**领域外**的例子（如测试支付流程却用邮件工具名） | 路径模拟的所有测试案例必须来自**被测试的同一领域** |
| 擅自回写陷阱表 | 发现坑后没问使用者，直接把坑写进被测试 skill | 回写有副作用（改文件），必须先问；第三方/共享 skill 默认只报告不写入 |
| 回写进 SKILL.md 正文 | 回写时把坑表写进 SKILL.md 主文件（book-distiller 实测：先内嵌后被纠正拆出）——使用 skill 时会加载维护向内容，且主文件被污染 | 一律回写进 references/（新建 common-pitfalls.md 或追加已有陷阱表文件），主文件只留引用；坑表是给修改者看的，不是给使用者看的 |
| 只修当下不沉淀 | 修完问题就停，新坑没进被测试 skill 的陷阱表 | 修完按步骤 7 询问是否回写；已有表先查重，无表则新建 references/common-pitfalls.md（不写进 SKILL.md 正文） |
| 未报告就修复 | 发现问题没先汇总给使用者就自动修 | 走完路径先出问题清单（步骤 4），使用者确认后才修（步骤 5）；"问题"可能是作者故意设计，直接修=替作者做设计决定 |
| 修复不询问 | 使用者说「只修 X」却全部修了 | 严格按使用者指定范围修；不确定就逐条确认 |
| 报告粒度不定 | 走完一条路径就急着报告，或全部走完才想起要报告——步骤 2/4 没说清是「每条路径报告」还是「3 条统一报告」 | 循环走完至少 3 条路径（标准 + 至少 1 条风险路径）、只记录不报告，全部走完统一汇总一份清单（已定稿：步骤 2 循环 + 步骤 4 统一汇总；模板含严重度列 + 走查轨迹字段） |
| 授权跳过报告无说明 | 使用者说「直接修不用问」，skill 却坚持强制报告或逐条确认，来回拉扯 | 使用者主动授权「直接修/不用问」→ 可跳过报告与逐条确认，直接批量修；修复记录仍写入产出物（步骤 5） |
| 部分修复后重复询问 | 使用者只修了部分条目，重走时未修的坑又冒出来被再次询问 | 部分修复后重走，未修条目标注「使用者选择不修（原问题 #N）」，不重复询问（步骤 6） |
| 拆分后引用断链 | 把章节拆到 references/ 后，主文件仍引用已不存在的章节（如「第 8 步产出物模板」→ 实际已在 references/report-template.md） | 拆分/重构后必走路径模拟的跨文件路径，专门检查主文件里对已移动章节的引用是否同步更新；「第 X 步」类编号引用最容易失效 |
| 知识错位进别的 skill 正文 | 发布流程知识（如「关于作者固定文案」）出现在业务 skill（如 book-distiller）的正文里，用户质问「明明是 A skill 的事情为什么在 B 里」（2026-08-25 实测） | 走查时对被测对象正文里的每个章节问「这属于这个 skill 的职责域吗？」——不属于的就是错位残留：确认权威位置已有完整版后删除，不复制搬运 |
| 判据维度单一漏场景 | 「带版本发布→支线」这类单维度判据，遇到新仓库类型（纯文档 skill 仓库）会给出过重结论（改两个 md 文件也要开支线），与实战惯例冲突（2026-08-26 实测） | 走查判据时问「换个被测对象类型这条还成立吗？」——判据要按对象类型分叉（代码仓库 vs 纯文档仓库），并写明各分支的理由 |
| 并发会话抢改被测对象，待修清单与磁盘脱节 | 走查报告列出 7 个问题，修复时发现一半已被并发会话修好（structure.py 单 h1 修复、test.py 17→29 用例、SKILL.md 升版），系统弹 sibling-modified 警告；照报告盲修=重复改或把对方修复改回去（2026-08-28 实测） | 修复动手前逐条 grep 磁盘确认问题仍成立；见 sibling-modified 警告必重读文件；对方已修的核实质量后采纳（汇报「已由并发会话修复，我核实保留」），不重复改、不抹对方注释与用例 |

---

## 方法论审查三问（方法论误移的独立入口）

路径模拟抓「实现错误」（流程断链、输入输出对不上）。但有些问题不是实现错了，是**整套方法论不匹配内容域**——走查走不出来，需要独立的设计层审查。当出现以下信号时，停下路径模拟，切换审查模式：

- 多条路径反复卡在同一处，即使按建议修复后重走仍卡（不是 step 写错，是路本身不对）
- 问题清单里混入「这个流程该不该存在」的疑问
- 被测对象的基本假设被路径推翻（如：用户根本不按步骤走，而是直接要结果）

此时问三问：

1. **这东西该做成 workflow/skill 吗？** — 有没有更轻的载体？（一段 FAQ、一份参考文档、一个自检清单，可能比完整流程更合适）
2. **目标用户真的是「走步骤」的人吗？** — 如果用户只会「直接给我结果」，步骤式设计本身就是错位
3. **有没有更合适的现有方案？** — 市面上或你库里是否已有同领域更成熟的流程，重复造轮子 = 方法论误移

三问回答后：仍要 workflow → 继续路径模拟；不要 → 记录「方法论不匹配」为独立审查项，**不修步骤**，回到定位重新设计。

---
