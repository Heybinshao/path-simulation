---
name: path-simulation
description: "【路径模拟自检法】写完/改完 workflow、skill、教程后，选真实用户指令模拟执行找卡点。触发词：路径模拟、走一遍、simulate workflow。适用：可执行流程类内容（skill/workflow/教程步骤）；不适用：纯知识类内容。"
version: 2.0.0
author: 彬少
platforms: [linux, macos, windows]
metadata:
  hermes:
    tags: [quality, verification, workflow, skill, testing]
    category: binshao
    triggers: [路径模拟, 走一遍, path simulation, simulate workflow]
---

# 路径模拟自检法

写完/改完 workflow、skill、教程、提示词规范后，不能只静态检查结构格式。选一条用户指令，按步骤实际模拟执行一遍，看哪里会卡住、哪里逻辑断链、哪里触发条件没覆盖。至少走 3 条不同路径（标准/跳过/中断），发现问题改完后再换路径重走。直到再走一条新路径不再发现新问题为止。

---

## 适用边界（先读这个）

| 适合 | 不适合 |
|------|--------|
| skill / workflow / 教程步骤 / 操作手册 | 纯知识类内容（百科条目、观点文章） |
| 有"用户从 A 到 B"流程的东西 | 没有执行步骤的参考文档 |
| 用 AI 写流程的人（agent 提示词、自动化脚本） | 只给别人读不给人用的文档 |

**一句话判断**：这东西有没有"用户会走的一串步骤"？有 → 值得路径模拟；没有 → 静态检查够了。

---

## 核心机制

**为什么叫「路径」？** 用户进入系统的方式不止一种：「直接执行」「先看方案再执行」「跳过某步」「中途反悔」——每一种用户意图，都是一条不同的路径。

**一句话定义：** 路径模拟就是用户可能的每种用法都跑一遍，看哪会卡住。

- 静态检查相信文档写清楚了。
- 路径模拟不相信——它要真的走一遍才信。

---

## 执行步骤

### 1. 选一条用户指令作为入口

从实际用户可能的说法里选，不要自创。常见入口：

- 标准路径：用户按文档预期的方式使用
- 跳过路径：用户跳过某步或直接要求结果
- 分批路径：用户分多次执行、中间插入其他请求
- 中断路径：用户中途反悔、改口、撤销
- 反悔路径：执行到一半用户说「不对，回退」

### 2. 按步骤实际走一遍

沿着规范每步往下走，走到哪发现：

- **卡住** — 这一步没有输入，或输入和上一步输出对不上
- **歧义** — 需要读者自己去猜，规范没说清
- **缺失** — 某个分支没覆盖

这些问题只在「真的走」时才暴露。走到每步时问自己：「这一步需要的输入从哪来？如果我是第一次用的用户，我能看懂吗？」

### 3. 记录每个问题

- 哪一步卡住
- 为什么卡（触发条件没写/输入缺失/输出对不上）
- 怎么修

### 4. 修完换路径重走

至少走 3 条不同路径（标准/跳过/中断）。不要只走一条就停。

### 5. 停下来的标准

**直到再走一条新路径，不再发现新问题为止。**

在此之前，答案永远是「再来一遍」。

---

## 7 条标准路径清单

| # | 路径 | 描述 | 典型用户话术 |
|---|------|------|-------------|
| 1 | 标准路径 | 按文档预期完整走一遍 | 「帮我做 X」 |
| 2 | 跳过路径 | 跳过一个前置步骤直接要结果 | 「直接给我结果，不用检查」 |
| 3 | 分批路径 | 任务拆成多次、中间夹其他请求 | 「先做 A」「顺便记一下 B」「继续 A」 |
| 4 | 中断路径 | 中途插入新指令打断原流程 | 「等一下，先处理这个」 |
| 5 | 反悔路径 | 执行到一半要回退 | 「那条别删」「恢复刚才改的」 |
| 6 | 异常路径 | 输入缺失/格式错误/环境不满足 | 「文件不存在」「没装依赖」 |
| 7 | 跨文件路径 | 多文件/多步骤协同场景 | 检查参考文件之间是否矛盾 |

---

## 和「自检清单」的关系

两者互补，不是替代：

- **自检清单** — 查单点（缩写/密度/违禁词）
- **路径模拟** — 查流程（触发条件/输入输出/断链/方法论不匹配）

顺序：自检清单查单点 → 路径模拟查流程 → 再自检清单确认。

---

## 常见坑

| 坑 | 表现 | 修法 |
|--|------|------|
| 静态审查通过就信了 | 结构完整就认为没问题 | 必须走一遍，至少 3 条路径 |
| 只走一条路径 | 标准路径通关就收工 | 换入口再走 |
| 走一遍没问题就停 | 第一次走通就完成 | "没发现问题"不是终点，再换路径 |
| 自检清单代替路径模拟 | 以为逐项打勾就够了 | 清单查单点，模拟查流程，两者缺一不可 |
| **测试域污染** | 模拟时用了**领域外**的例子（如测试支付流程却用邮件工具名） | 路径模拟的所有测试案例必须来自**被测试的同一领域** |
| 方法论误移 | 发现的问题不是"实现错误"，而是整套方法论不匹配内容域 | 此时修的不是 workflow 步骤，是 workflow 本身的设计前提。回到被测试对象的定位重新评估——这条路走不通不是因为 step 写错了，是因为路本身就不该修成这个样子。记录方法论不匹配作为独立的审查项 |
| 时序错位 | 检查/确认类步骤排在**写入/分发之后**，等于没查（如覆盖确认写在最后一步，但文件在第二步就已被写完） | 检查类步骤必须排在对应写入/分发**之前**。写完再查的「检查」是装饰 |
| 跨文件矛盾 | 每条路径都走通了，但相关参考文件之间互相矛盾（A 文件说允许、B 文件说禁止） | 至少检查一次**跨文件一致性**：走完 N 条路径后，专门检查多个参考文件中描述同一概念的部分 |
| 主体可移除测试 | 去掉工作流中的某个角色/步骤后整个流程依然成立 | 该元素是装饰性的——重新设计：移除或让它承担真正的功能 |
| 异常表误导 | 异常处理表声称「A 正常」，但真实路径下 A 也出错 | 异常表描述必须来自实测，不能凭想象写；重走路径时专门验证异常表里的"正常"声明 |

---

## 产出物

每条路径模拟结束后输出：

```
## 路径模拟报告

### 测试对象
<skill/workflow 名称 + 版本>

### 覆盖路径
标准 / 跳过 / 中断 / 分批 / 反悔 / 异常 / 跨文件（勾选实际走的）

### 问题清单
| # | 路径 | 问题 | 哪步卡 | 为什么 | 修复 |
|---|------|------|--------|--------|------|
| 1 | 标准 | 检查步骤排在写入后 | 第 4 步 | 时序错位 | 移到第 2 步前 |

### 重走验证
- 修复后重走路径：标准 ✅ / 跳过 ✅ / 中断 ✅
- 新路径（异常）发现新问题：无 → 停止

### 结论
<通过 / 未通过 + 剩余问题>
```

---

## 常见问题（FAQ）

**Q: 我改的是一个很小的脚本，也要走 3 条路径吗？**
A: 越小越要快——3 条路径 5 分钟走完。问题是"有没有走"，不是"走了几条"。

**Q: 我是人类，不是 agent，怎么"模拟执行"？**
A: 把步骤念出来，假装自己是第一次用的用户，每一步问「我现在该干什么？我知道吗？」。卡住的地方就是文档没写清的地方。

**Q: 路径模拟和实际测试有什么区别？**
A: 实际测试跑真实环境；路径模拟在脑子里/纸面上走。路径模拟便宜（零成本），能发现 80% 的流程问题，剩下的交给真实测试。

**Q: 走完 3 条路径没发现问题，能保证没问题吗？**
A: 不能。但"换一条新路径不再发现问题"是合理的停止点——继续走边际收益递减。
